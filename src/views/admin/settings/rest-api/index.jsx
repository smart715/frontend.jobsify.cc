
'use client'

import { Card, CardContent, Typography } from '@mui/material'

const RestApi = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6" sx={{ mb: 4 }}>
          Rest API Setting
        </Typography>
        <Typography variant="body1">
          REST API settings configuration will go here.
        </Typography>
      </CardContent>
    </Card>
  )
}

export default RestApi
