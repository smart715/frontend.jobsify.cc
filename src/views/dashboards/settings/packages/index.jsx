// src/views/dashboards/settings/packages/index.jsx

import React from 'react';

import Typography from '@mui/material/Typography';

import Card from '@mui/material/Card';

import CardContent from '@mui/material/CardContent';

const PackagesSettings = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant='h5'>Packages Settings</Typography>
        <Typography variant='body1'>
          This is a placeholder for the Packages settings content.
          Packages configuration will be managed here.
        </Typography>
      </CardContent>
    </Card>
  );
};

export default PackagesSettings;
